// If needed, implement the necessary configuration here

// Example content:
const UPLOAD_CONFIG = {
    maxFileSize: 100 * 1024 * 1024, // 100 MB
    acceptedFileTypes: ".jpg,.jpeg,.png,.gif,.mp3,.wav,.txt,.obj,.stl,.off,.ply"
};